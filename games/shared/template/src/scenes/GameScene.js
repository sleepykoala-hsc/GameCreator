// GameScene.js — Core gameplay loop.
// Unity equivalent: your main gameplay Scene with Update() / FixedUpdate().

class GameScene extends Phaser.Scene {
  constructor() {
    super({ key: 'GameScene' });
  }

  create() {
    // TODO: set up game world, spawn entities
  }

  update(time, delta) {
    // TODO: handle input, update entities, check win/lose conditions
  }
}
